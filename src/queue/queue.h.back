#ifndef __QUEUE_H_
#define __QUEUE_H_

typedef struct {
  int *array;
  int front;
  int back;
  int size;
  int maxSize;
} * Queue;

Queue init(const int size);
int enqueue(Queue q, int v);
int dequeue(Queue q, int *v);
int front(Queue q, int *v);
int isEmpty(Queue q);
#endif
